{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "check-types": "tsc -b",

    "adev": "alchemy dev",
    "aclean": "rm -rf .alchemy .wrangler",

    "dev": "wrangler dev --port=3000",
    "build": "tsdown",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "hono": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/zod": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@datango/api": "workspace:*",
    "@datango/auth": "workspace:*",
    "@datango/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsdown": "catalog:",
    "@datango/config": "workspace:*",
    "alchemy": "catalog:",
    "wrangler": "^4.40.3",
    "@types/node": "catalog:",
    "@cloudflare/workers-types": "^4.20250822.0"
  }
}
